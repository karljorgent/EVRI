import styles from "@/styles/Showcase.module.css";
import { supabase } from "../supabaseClient";

function Showcase({ teavikud_info }) {
  return (
    <div>
      <h1>MY BALLS</h1>
    </div>
  );
}

export default Showcase;
